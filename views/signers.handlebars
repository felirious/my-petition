<!DOCTYPE html>
    <body>
        <div class="middlestuff">

            <div class="middlemiddle">
                <h2>These famous people have already signed:</h2>
                <br>
                <br>
                {{#rows}}
                    <ul>
                        <li><a href='{{homepage}}'>{{first}} {{last}}</a>, <a href='/signers/{{city}}'>{{city}}</a>, {{age}}
                        </li>
                    </ul>
                {{/rows}}
            </div>
        </div>
    </body>
